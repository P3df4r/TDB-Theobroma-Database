"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const react_1 = __importDefault(require("react"));
const mobx_react_1 = require("mobx-react");
const configuration_1 = require("@jbrowse/core/configuration");
const Arrow = ({ feature, featureLayout, config, region, }) => {
    const strand = feature.get('strand');
    const size = 5;
    const reverseFlip = region.reversed ? -1 : 1;
    const offset = 7 * strand * reverseFlip;
    const { left = 0, top = 0, width = 0, height = 0 } = featureLayout.absolute;
    const color2 = (0, configuration_1.readConfObject)(config, 'color2', { feature });
    const p = strand * reverseFlip === -1
        ? left
        : strand * reverseFlip === 1
            ? left + width
            : null;
    const y = top + height / 2;
    return p ? (react_1.default.createElement(react_1.default.Fragment, null,
        react_1.default.createElement("line", { x1: p, x2: p + offset, y1: y, y2: y, stroke: color2 }),
        react_1.default.createElement("polygon", { points: [
                [p + offset / 2, y - size / 2],
                [p + offset / 2, y + size / 2],
                [p + offset, y],
            ].toString(), stroke: color2 }))) : null;
};
exports.default = (0, mobx_react_1.observer)(Arrow);
//# sourceMappingURL=Arrow.js.map