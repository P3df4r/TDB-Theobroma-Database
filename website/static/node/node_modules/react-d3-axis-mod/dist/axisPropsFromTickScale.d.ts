import { ScaleLinear, ScaleQuantize } from 'd3-scale';
export default function axisPropsFromTickScale(scale: ScaleLinear<number, number, never> | ScaleQuantize<number, never>, tickCount: number): {
    range: number[];
    values: number[];
    format: (d: import("d3-scale").NumberValue) => string;
    position: ScaleLinear<number, number, never> | ScaleQuantize<number, never>;
};
